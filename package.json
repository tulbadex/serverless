{
  "name": "serverlessq",
  "version": "1.0.0",
  "description": "serverlessq interview test using api gateway and lambda",
  "main": "api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "tulbadex",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.118.1",
    "@aws-sdk/util-dynamodb": "^3.118.1",
    "serverless-iam-roles-per-function": "^3.2.0"
  }
}
